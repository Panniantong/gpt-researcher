OPENAI_API_KEY=
TAVILY_API_KEY=
DOC_PATH=./my-docs

# PDF Generation Settings
# Set to false to disable PDF generation (useful on macOS if WeasyPrint dependencies are not installed)
ENABLE_PDF_GENERATION=true

# NEXT_PUBLIC_GPTR_API_URL=http://0.0.0.0:8000  # Defaults to localhost:8000 if not set